#include <random>
#include <set>


class ScenarioGenerator{

	private:
		std::mt19937 generator;
		
		const std::shared_ptr<Quantlib::OneFactorAffineModel> model_;
		std::vector<std::vector<double>> scenarios;
	public:
		ScenarioGenerator(const std::set<int> indices,
				std::shared_ptr<Quantlib::OneFactorAffineModel> model,
			       	int n);
		~ScenarioGenerator();

}

